apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
spec:
  type: LoadBalancer  # Or NodePort for local development
  selector:
    app: fastapi-sqlite-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
